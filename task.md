# 任务跟踪清单 (task.md) - GitHub Issue #13

- [x] 步骤 1：环境检视与分支创建
  - [x] 验证最新 main 基线，本地全量测试通过（150 tests pass）
  - [x] 创建并切换到新分支 `feat/13-batch-operations-genealogy-edges`
- [x] 步骤 2：数据库迁移脚本编写与验证
  - [x] 编写 Flyway 脚本 `server/src/main/resources/db/migration/V4__batch_operation_constraints.sql`
  - [x] 扩展创建幂等键长度至 128 字符，修改全局唯一为同组织唯一索引 `uk_op_org_idempotency`
  - [x] 新增提交幂等键 `submission_idempotency_key` 及其同组织唯一索引 `uk_op_org_submission_idempotency`
  - [x] 为 `batch_operation`、`batch_operation_item`、`batch_relation` 补充物理 CHECK 约束（含操作类型、状态、角色、正数量、kg单位、关系类型、非自环）
  - [x] 验证 Flyway 迁移脚本语法与执行无误
- [x] 步骤 3：领域模型、DTO 与数据持久层（Mapper/XML）实现
  - [x] 实体类与枚举：`BatchOperation`、`BatchOperationItem`、`BatchRelation`、`BatchOperationType`、`BatchOperationStatus`、`BatchItemRole`、`BatchRelationType`
  - [x] DTO 定义：`BatchOperationCreateRequest`、`BatchOperationItemRequest`、`BatchOperationSubmitRequest`、`BatchOperationResponse`、`BatchOperationItemResponse`、`BatchRelationResponse`
  - [x] Mapper 接口与 MyBatis XML：
    - [x] `BatchOperationMapper`（创建幂等查询、提交幂等查询、排他锁行读、提交更新原子操作）
    - [x] `BatchOperationItemMapper`（批量插入、按操作查询明细、查询历史累计已提交 INPUT 数量）
    - [x] `BatchRelationMapper`（批量插入边、查询上游产出边、**MySQL 8.4 recursive CTE 环检测查询**）
- [x] 步骤 4：应用服务层业务逻辑实现（`BatchOperationApplicationService`）
  - [x] 创建批次操作草稿：
    - [x] `OPERATOR` 权限复核，服务端组织上下文与当前时间绑定
    - [x] 幂等键基础校验（16..128），载荷语义比对与冲突诊断
    - [x] 并发 `DuplicateKeyException` 捕获与当前读恢复
    - [x] 操作类型基数检查（MERGE 至少2入1出；SPLIT 至少1入2出；PROCESS/REPACK 至少1入1出）
    - [x] 项目校验（角色与 batchId 绑定规则、正数量、最多3位小数、单位为 kg、同一操作中 batchId 唯一且不得兼作入出）
    - [x] 插入操作与明细
  - [x] 提交批次操作：
    - [x] 仅本组织 `OPERATOR` 权限校验
    - [x] 提交幂等键格式校验（16..128）与请求体 `version` 校验
    - [x] 提交幂等已完成直接重放；草稿状态校验（非 DRAFT 报 409）
    - [x] **稳定顺序排他锁**：先锁操作，再对所有关联批次按 `batchId` 升序排他行锁
    - [x] 批次活性校验：全部存在、属于本组织、状态为 `ACTIVE`（非 ACTIVE 报 422 `BATCH_FLOW_BLOCKED`）
    - [x] **物料平衡计算**：`sum(INPUT) = sum(OUTPUT) + sum(LOSS) + sum(WASTE) + sum(SAMPLE)`，容差 0.001kg，超差报 422 `BATCH_MASS_BALANCE_VIOLATION`
    - [x] **输出批次唯一生产来源校验**：输出批次无上游谱系边（已有报 422），且输出数量严格等于批次声明数量（不符报 422）
    - [x] **输入批次历史累计量校验**：历史已提交 INPUT 数量 + 本次 INPUT 数量 <= 批次声明数量（超出报 422 `BATCH_QUANTITY_EXCEEDED`）
    - [x] **笛卡尔积生成关系边与自环校验**：INPUT × OUTPUT 笛卡尔积生成边，父子相同报 422 `BATCH_RELATION_SELF_LOOP`
    - [x] **MySQL 8.4 recursive CTE 成环检测**：检查拟增 child 是否已可达 parent，若成环报 422 `BATCH_RELATION_CYCLE`
    - [x] 原子流转状态为 `SUBMITTED`，写入提交幂等键，乐观锁版本自增，写入谱系边，任一失败完整回滚
- [x] 步骤 5：表现层控制器实现（`BatchOperationController`）
  - [x] `POST /api/v1/batch-operations` 端点
  - [x] `POST /api/v1/batch-operations/{operationId}/submit` 端点
  - [x] 白名单响应，绝不暴露内部软删除或幂等键字段，时间格式带明确 UTC 偏移量
- [x] 步骤 6：文档同步与批次数量歧义修正
  - [x] 同步更新 `docs/api/openapi.yaml`，补齐 `/batch-operations` 与 `/batch-operations/{operationId}/submit` 的全量契约与响应码
  - [x] 修正所有文档中关于 `batch.quantity`“当前数量”的歧义描述为“初始/声明数量，可用量派生”
  - [x] 编写 `docs/phase3/README.md` 第四部分（操作规则、物料平衡、环检测、并发与幂等机制）
  - [x] 更新 `README.md` Phase 3 进展
- [x] 步骤 7：全面单元测试与 Web 契约测试
  - [x] 编写 `BatchOperationApplicationServiceTest`（覆盖各种边界、业务规则校验、基数、物料平衡、环检测 Mock、幂等分支，21 个用例全部通过）
  - [x] 编写 `BatchOperationControllerTest`（覆盖权限、CSRF、参数拦截、白名单响应、时间格式契约，9 个用例全部通过）
- [x] 步骤 8：真实 MySQL 8.4 集成测试与验证
  - [x] 编写 `BatchOperationMysqlIntegrationTest`：
    - [x] 标准链端到端验证（600kg+420kg -> 480kg+520kg+20kg 损耗，生成 4 条边）
    - [x] 失败场景：物料不平衡回滚、环检测回滚、自环拦截、输入超额拦截、输出已有上游拦截、非 ACTIVE 拦截
    - [x] 跨组织隔离与非 OPERATOR 拦截
    - [x] 并发双提交与幂等重放验证
    - [x] MySQL 8.4 物理 CHECK 约束拦截验证
    - [x] 物理数据清理
  - [x] 验证默认测试套件全绿（186 tests run, 18 skipped, 0 failures, 0 errors）
  - [x] 开启 `MYSQL_IT_ENABLED=true` 运行全量集成测试，达到 186 passed, 0 failures, 0 errors, 0 skipped
- [x] 步骤 9：自我审查、Git 提交、推送与 PR 创建
  - [x] 严格对照 Issue #13 逐项审查
  - [x] git commit 规范提交（Commit: c0a96d5）
  - [x] git push 并创建 Pull Request（PR #14: https://github.com/zamatewi-cell/frozen-seafood-traceability/pull/14），写明 Closes #13、实现边界、迁移、测试结果与已知非目标
  - [x] 最终按规范格式报告

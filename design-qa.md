# Phase 1 设计验收报告

- 验收日期：2026-09-03
- 参考源：`docs/design/assets/phase1-trace-reference.png`
- 最终实现截图：`prototype/qa/trace-final.png`
- 补充状态截图：`prototype/qa/quality-final.png`、`prototype/qa/consumer-mobile.png`
- 浏览器：Playwright Chromium
- 桌面视口：1487 × 1058 CSS px，截图密度 1×
- 移动视口：390 × 844 CSS px，截图密度 1×
- 验收状态：批次追溯默认态；质量事件处置完成态；消费者有效码与模拟召回态

## 对照结论

### 整体视图

参考源与最终实现均为 1487 × 1058，已在相同视口和默认批次状态下逐屏对照。实现保留了参考源的深色左侧导航、白色顶部工具条、追溯搜索区、网状批次图谱、右侧详情与摘要、分环节温控区以及模拟数据声明。信息层级、主色、危险/成功状态色、卡片边界和主要间距一致，无裁切和桌面纵向滚动。

实现中的 X6 节点采用可交互矩形卡片，视觉密度低于参考源的静态行业图标卡片；这是 Phase 1 原型为保证节点下钻与后续动态拓扑扩展做出的可接受简化，不影响信息架构、业务含义或关键任务完成。

### 关键区域

全屏截图已按原始分辨率保存，批次节点、边标签、双时间戳与温控规则均可直接辨认，因此未另行裁切局部图。重点核对结果如下：

- 网状谱系同时显示两个上游原料、一个加工批次和两个下游流通批次。
- 连线明确标注“多源合批”和“批次拆分”。
- 数量满足 `1,020 kg - 20 kg = 1,000 kg`，下游 `480 kg + 520 kg = 1,000 kg`。
- 两个原料均为扇贝，产出为冷冻扇贝柱，物种一致。
- 温控采用按环节规则，不使用贯穿全链路的单一 `-18 ℃` 判定线。
- 详情同时展示业务发生时间与系统记录时间。
- 页面显式标注“数据来源：模拟数据（非真实物联网设备）”。

## 缺陷与修复记录

| 轮次 | 级别 | 发现 | 处理结果 |
|---|---|---|---|
| 初检 | P1 | 图谱未显式标注合批和拆分操作 | 已为 X6 连线增加“多源合批”“批次拆分”标签 |
| 初检 | P2 | 默认桌面页超出目标视口约 25 px | 已压缩图谱、温控区和页面纵向间距 |
| 初检 | P2 | favicon 请求产生控制台 404 | 已使用内联空 favicon 消除请求 |
| 复检 | P2 | 页面仍有约 10 px 纵向溢出 | 已调整内容区纵向 padding；最终 `scrollHeight = innerHeight = 1058` |
| 复检 | P1 | 全局最小宽度导致消费者页在手机视口横向溢出 | 已将最小宽度约束收敛到企业端，并增加 390 px 移动布局 |
| 交互检 | P1 | 模拟召回已触发但步骤仍显示等待操作 | 已补齐完成态：四步全部留痕、事件状态“已处置”、按钮禁用 |

最终未遗留 P0、P1 或 P2 缺陷。

## 交互验收

| 场景 | 结果 |
|---|---|
| 点击原料节点下钻详情 | 通过；详情切换为 `RAW-20260831-017`，并显示原料批次双时间戳 |
| 输入无效追溯码 `BAD-001` | 通过；显示“未找到该追溯码，请检查后重试” |
| 批次档案 | 通过；显示合批构成、双时间戳、关联证据和温控/检验摘要 |
| 新建加工批次向导 | 通过；四步可连续完成，最终进入“批次草稿已创建”状态 |
| 质量事件四步闭环 | 通过；确认、冻结、范围、模拟召回全部留痕，最终为“已处置” |
| 消费者有效码状态 | 通过；仅展示公开字段、关键履历、温控摘要和教学声明 |
| 消费者模拟召回状态 | 通过；展示暂停食用提示和模拟召回声明 |
| 390 × 844 移动适配 | 通过；无横向溢出，内容与声明完整可见 |

## 工程与控制台检查

- 浏览器控制台：0 errors，0 warnings。
- `npm run build`：通过；仅有原型依赖导致的 bundle 体积提示，后续生产前端需按页面懒加载 X6/ECharts。
- `npm run test:sites`：4/4 通过。
- OpenAPI lint：通过，0 warnings。
- `git diff --check`：通过。

final result: passed
